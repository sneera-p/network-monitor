# Generic ignores
# Ignore files that are generated by the OS
.DS_Store
Thumbs.db
*.swp
*~
.idea/ # Ignore JetBrains IDE settings

# Build/Compile Artifacts
# C/C++ build artifacts
*.o
*.lo
*.la
*.al
*.aux
*.o.d
*.lib # Windows static libs
*.obj # Windows objects
*.pdb # Debug symbols
*.ilk # Incremental linker files

# Compiled Shared Libraries (adjust extensions for your OS)
*.so
*.dll
*.dylib
*.wasm

# Protobuf Generated Files (if not tracked as source)
# If you *do* track generated proto files, remove these lines
# Core/generated/ # Example if generated files go into a specific folder
*.pb.c
*.pb.h
*_pb2.py
*.g.cs
*.ts

# Python ignores
__pycache__/
*.pyc
*.pyd
*.pyo
.Python
env/
venv/
build/
develop-eggs/
dist/
eggs/
.eggs/
parts/
sdist/
var/
*.egg-info/
.installed.cfg
*.manifest
*.spec
pip-log.txt
pip-delete-this-directory.txt
.project
.dmypy.json
.mypy_cache/
.pytest_cache/
.coverage
.cache/

# .NET / C# ignores
[Bb]in/
[Oo]bj/
*.csproj.user
*.vs/
*.suo
*.user
*.userosf
*.gmlaas
*.opensdf
*.sln.idevars

# NuGet package directory
[Pp]ackages/

# Node.js / TypeScript / SolidJS / Vite ignores (Web/)
node_modules/
dist/
.cache/
.temp/
.local/

# Dependency directories (can vary based on lock files, but node_modules is standard)
jspm_packages/

# Logs and temporary files
*.log
*.tmp
*.temp

# dotenv files (contains secrets)
.env
.env.*

# Reports generated by Reporter
# Be careful if you need to track *some* reports, but generally generated output is ignored
Report/output/ # Example output directory
*.pdf # If they are generated directly in the Reporter folder

# SurrealDB (if it creates local files, usually managed by Docker)
# If you have local SurrealDB files outside Docker, add ignores here
dump.surql # Example dump file

# Docker ignores (if you have specific Docker build/run files to ignore)
.dockerignore # (this file itself should not be ignored)